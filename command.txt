python main.py  - - - - - - -- -- - - -

pyinstaller --noconfirm --onefile --windowed main.py --name "All_in_One"

# Знаходимо хеш коміту (1a2b3c4 з прикладу вище)
    git log --oneline

# Повертаємося до цього коміту
    git checkout 1a2b3c4

# Або створюємо нову гілку з цим станом
    git checkout -b recovery-branch 1a2b3c4

# Переглядаємо історію комітів
    git log --oneline --graph

# Буде виведено щось на кшталт:
    # * 1a2b3c4 (HEAD -> main) Сейвпоінт 2: Український календар...
    # * 5d6e7f8 Сейвпоінт 1: Початкова версія програми



# Перевіряємо зміни
    git status

# Додаємо всі зміни
    git add .

# Створюємо коміт
    git commit -m "Сейвпоінт 3: Працюючий календар з українською локалізацією та замітками"

# Відправляємо на GitHub
    git push origin main


    pyinstaller --noconfirm --onedir --windowed --icon=icons/app_icon.ico --name="AllInOne" main.py
